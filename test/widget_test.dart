// KakeiboPro — Placeholder de tests de widget
//
// Este archivo reemplaza el test de contador del template de Flutter,
// que no aplica a la arquitectura de KakeiboPro.
//
// Los tests reales se agregarán en test/features/ conforme avance el desarrollo.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — suite lista para tests reales', () {
    expect(true, isTrue);
  });
}
